apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: knative-func-backstage-templates
  description: A collection of all the templates for the Knative Functions
spec:
  targets:
    - ./node-cloudevents-template.yaml
    - ./node-http-template.yaml
